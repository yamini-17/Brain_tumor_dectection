╔════════════════════════════════════════════════════════════════════════════════╗
║                  BRAIN TUMOR DETECTION SYSTEM - QUICK START                    ║
╚════════════════════════════════════════════════════════════════════════════════╝

🎯 FASTEST WAY TO START
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Windows:
  1. Double-click: START_SYSTEM.bat
  2. Wait for setup
  3. Browser opens to http://localhost:3000

macOS/Linux:
  1. chmod +x start_system.sh
  2. ./start_system.sh
  3. Open http://localhost:3000

Manual (Any OS):
  Terminal 1:  cd backend   && python app.py
  Terminal 2:  cd frontend  && npm install && npm run dev
  Browser:     http://localhost:3000


📋 SYSTEM REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ Python 3.8+       (Download: https://python.org)
✓ Node.js 16+       (Download: https://nodejs.org)
✓ 8 GB RAM minimum
✓ 5 GB disk space
✓ GPU optional (much faster with CUDA)


📂 PROJECT STRUCTURE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

yolov9/
├── backend/                 ← Flask API server (port 5000)
│   ├── app.py
│   ├── requirements.txt
│   ├── model/
│   └── utils/
│
├── frontend/                ← React app (port 3000)
│   ├── package.json
│   ├── src/
│   │   ├── components/      ← UI components
│   │   ├── styles/          ← CSS files
│   │   ├── utils/           ← API integration
│   │   └── App.jsx          ← Main app
│   └── vite.config.js
│
├── START_SYSTEM.bat         ← Click to start (Windows)
├── start_system.sh          ← Run to start (macOS/Linux)
└── COMPLETE_SETUP_GUIDE.md  ← Full documentation


🌐 URLS & PORTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Frontend:     http://localhost:3000     (React app)
Backend API:  http://localhost:5000     (Flask server)
API Docs:     http://localhost:5000/    (API help)


🔌 API ENDPOINTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

GET  /health             → Check if server is running
POST /predict            → Analyze MRI image (main endpoint)
GET  /status             → Get system information


💾 KEY CONFIGURATION FILES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Backend:
  backend/.env            ← Model path, file size limits
  backend/app.py          ← Main Flask server

Frontend:
  frontend/.env           ← API URL configuration
  frontend/package.json   ← Dependencies & scripts


🐛 TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❌ "Port already in use" error?
   → Close other app using that port, or:
   → Change port in vite.config.js or app.py

❌ "Module not found" error?
   → Reinstall: pip install -r requirements.txt (backend)
   → Reinstall: npm install (frontend)

❌ Backend won't start?
   → Check Python version: python --version
   → Clear cache: rm -rf backend/__pycache__
   → Reinstall: pip install --force-reinstall -r requirements.txt

❌ Frontend won't start?
   → Check Node version: node --version
   → Clear cache: rm -rf frontend/node_modules
   → Reinstall: npm install

❌ Can't connect to backend?
   → Make sure backend is running on port 5000
   → Check firewall settings
   → Check .env file has correct REACT_APP_API_URL

❌ Image upload fails?
   → Image must be JPG, PNG, BMP, GIF, or TIFF
   → File size must be under 10 MB


✅ TESTING WORKFLOW
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. ✓ Both servers running (no errors in terminal)
2. ✓ Browser opens http://localhost:3000
3. ✓ Upload an MRI image
4. ✓ Click "Analyze MRI Image" button
5. ✓ Results appear within 30 seconds
6. ✓ Shows detection status & confidence
7. ✓ Bounding box visible if tumor detected


⚡ PERFORMANCE TIPS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ First run is slower (model loading)
✓ CPU processing: 30-60 seconds per image
✓ GPU processing: 0.5-2 seconds per image
✓ GPU acceleration requires NVIDIA CUDA
✓ Install pytorch-cuda for GPU support


📦 COMMON COMMANDS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Backend:
  cd backend
  pip install -r requirements.txt    # Install deps
  python app.py                      # Start server
  python test_api.py                 # Test API

Frontend:
  cd frontend
  npm install                        # Install deps
  npm run dev                        # Start dev server
  npm run build                      # Build for production
  npm run preview                    # Preview production build


🚀 PRODUCTION DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Build Frontend:
  cd frontend
  npm run build                      # Creates dist/ folder

Deploy Options:
  • Vercel (frontend)
  • Netlify (frontend)
  • AWS S3 + CloudFront (frontend)
  • Heroku (backend)
  • AWS EC2 (both)
  • Docker (both)


📚 DOCUMENTATION FILES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📄 COMPLETE_SETUP_GUIDE.md    ← Full setup & configuration guide
📄 backend/README.md          ← Backend documentation
📄 backend/QUICKSTART.md      ← Backend quick start
📄 frontend/README.md         ← Frontend documentation
📄 frontend/QUICKSTART.md     ← Frontend quick start


🔗 USEFUL LINKS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Python:          https://www.python.org
Node.js:         https://nodejs.org
React Docs:      https://react.dev
Flask Docs:      https://flask.palletsprojects.com
YOLOv9 Docs:     https://docs.ultralytics.com/models/yolov9/
Vite Docs:       https://vitejs.dev


📞 NEED HELP?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Check COMPLETE_SETUP_GUIDE.md for detailed help
2. Review console errors (press F12 in browser)
3. Check terminal output for error messages
4. Verify all prerequisites are installed
5. Restart both services


⏱️  TYPICAL STARTUP TIME
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Dependencies install:  2-5 minutes (first time only)
2. Backend startup:       5-10 seconds
3. Frontend startup:      3-5 seconds
4. Model loading:         10-30 seconds (first prediction)
5. Ready to use:          ~30 seconds total


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✨ READY TO START?

  Windows:   Double-click START_SYSTEM.bat
  macOS/Linux: ./start_system.sh
  Manual:    Follow "FASTEST WAY TO START" above

  Then open: http://localhost:3000

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
